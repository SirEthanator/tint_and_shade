[package]
name = "tint-and-shade"
description = "A minimal CLI for generating tints and shades from any color."
version = "1.0.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.38", features = ["derive"] }
cli-clipboard = "0.4.0"
term_size = "0.3.2"
